<br/>
<div class="card">
    <div class="card-header bi bi-chat-right-dots">댓글 작성</div>
    {{! 댓글작성 부분}}
    <form>
        {{#userName}}
            <input type="hidden" id="postId" value="{{post.id}}">
            <input type="hidden" id="commentAuthor" value="{{userName}}">
            <input type="hidden" id="userEmail" value="{{userEmail}}">
            <div class="card-body">
                <textarea id="commentContent" class="form-control" rows="4" placeholder="댓글을 입력하세요"></textarea>
            </div>
            <div class="card-footer">
                <button type="button" id="btn-comment"class="btn btn-outline-primary bi bi-pencil-square">등록</button>
            </div>

        {{/userName}}
        {{^userName}}
            <div class="card-body" style="font-size: small"><a href="/auth/login">로그인</a>을 하시면 댓글, 추천을 등록할 수 있습니다.</div>
        {{/userName}}
    </form>
    {{#userName}}
        <button type="button" id="btn-heart"class="btn btn-outline-primary bi bi-pencil-square">추천</button>
    {{/userName}}
</div>